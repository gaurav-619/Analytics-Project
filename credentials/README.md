# Credentials (do not commit)

This project expects a Google Cloud service account key JSON **locally** at:

- `credentials/service-account.json` (ignored by git)

Recommended:

- Set `GOOGLE_APPLICATION_CREDENTIALS` to the full path of your key JSON.
- Rotate/revoke any key that was previously committed or shared.

